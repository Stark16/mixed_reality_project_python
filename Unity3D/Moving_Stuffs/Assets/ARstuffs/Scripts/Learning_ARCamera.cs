using UnityEngine.XR.ARFoundation;
using UnityEngine;



public class Learning_ARCamera : MonoBehaviour
{
    [SerializeField]
    ARCameraManager ARCamera;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }
 
    // Update is called once per frame
    void Update()
    {

        //Debug.Log("Hi");
    }
    
}
